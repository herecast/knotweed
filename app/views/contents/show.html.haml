.row
  .span2
    .well
      %h4 Entities
  .span8
    %h1 
      = @features["TITLE"]
    %h5 
      = simple_format @features["AUTHORS"]
    %p
      = @features["LOCATION"] + " -- " if @features["LOCATION"].present?
      = simple_format @content.gsub("\u2028", "\n\n") # simple_format doesn't catch \u2028
  .span2
    .well
      %h4 More
