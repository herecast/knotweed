= @market_post.content.title
\
= raw strip_tags(@body)
\
- if @market_post.cost.present?
  Cost: #{@market_post.cost}
  \
- contact_string = market_contact_display(@market_post)
- if contact_string.present?
  Contact: #{contact_string}
  \
- if @market_post.locate_address.present?
  Where: #{@market_post.locate_address}
  \
\
More details here - #{@market_post.content.short_link}
