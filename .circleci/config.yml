version: 2

jobs:
  build:
    working_directory: ~/knotweed
    docker:
      - image: circleci/ruby:2.3.3-node
        environment:
          BUNDLE_PATH: vendor/bundle
          PGHOST: 127.0.0.1
          PGUSER: knotweed
          RAILS_ENV: test
          TZ: "/usr/share/zoneinfo/America/New_York"
      - image: circleci/postgres:9.5
        environment:
          POSTGRES_USER: knotweed
          POSTGRES_DB: knotweed_test
          POSTGRES_PASSWORD: ""
      - image: elasticsearch:2.3
      - image: circleci/redis
    steps:
      - checkout

      - restore_cache:
          keys:
            - knotweed-bundle-{{ checksum "Gemfile.lock" }}
            - knotweed-bundle-

      - run:
          name: Bundle install
          command: bundle install

      - save_cache:
          key: knotweed-bundle-{{ checksum "Gemfile.lock" }}
          paths:
            - vendor/bundle

      - run:
          name: Copy DB info
          command: mv config/database.yml.docker config/database.yml


      - run:
          name: Wait for DB
          command: dockerize -wait tcp://localhost:5432 -timeout 1m

      - run:
          name: Database setup
          command: |
            bundle exec rake db:create
            bundle exec rake db:schema:load

      - run:
          name: Run tests
          command: COVERAGE=true bundle exec rspec
